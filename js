body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f4f4f4;
}

header {
    background: #4CAF50;
    color: white;
    padding: 10px 0;
    text-align: center;
}

main {
    padding: 20px;
}

.info {
    background: white;
    padding: 20px;
    border-radius: 5px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

button {
    background-color: #4CAF50;
    color: white;
    border: none;
    padding: 10px 15px;
    cursor: pointer;
    border-radius: 5px;
    margin-top: 10px;
}

button:hover {
    background-color: #45a049;
}

footer {
    text-align: center;
    padding: 10px 0;
    background: #4CAF50;
    color: white;
    position: relative;
    bottom: 0;
    width: 100%;
}

#crop-health-id {
    font-weight: bold; /* Make the Crop Health ID stand out */
    color: #FF5722; /* Change color for visibility */
}

#photo-preview {
    display: none; /* Initially hide the preview */
    width: 200px; /* Set a width for the preview */
    margin-top: 10px; /* Add some space above the preview */
    border: 1px solid #ccc; /* Add a border for better visibility */
    border-radius: 5px; /* Round the corners */
}
